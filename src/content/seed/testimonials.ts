/**
 * Testimonials seed.
 *
 * Empty by design. A testimonial needs a real founder quote *and* recorded
 * consent (§16.5); neither exists in the audit material. The `testimonials`
 * feature flag stays off (§30).
 */

import type { Testimonial } from "../types";

export const SEED_TESTIMONIALS: Testimonial[] = [];
