/**
 * Network seed.
 *
 * Empty by design. §14: publish only if Foundry has a real, approved network to
 * show. Operating partners, advisors and angels are not published anywhere in
 * the audit material, and Luminar's person register must not be copied (§32).
 */

import type { NetworkPerson } from "../types";

export const SEED_NETWORK_PEOPLE: NetworkPerson[] = [];
